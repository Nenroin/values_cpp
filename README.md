### **1️⃣ Обмен значений**

📌 **Задание:**
Объявите две целочисленные переменные `a` и `b`. Присвойте им значения 5 и 10 соответственно. Затем поменяйте их значения местами без использования третьей переменной.

✅ **Пример работы:**

```
Исходно: a = 5, b = 10
После обмена: a = 10, b = 5
```

---

### **2️⃣ Тройной обмен**

📌 **Задание:**
Объявите три переменные: `x = 1`, `y = 2`, `z = 3`. Поменяйте значения так, чтобы `x` получил значение `y`, `y` - значение `z`, а `z` - значение `x`. Используйте только операции присваивания.

✅ **Пример работы:**

```
До: x=1, y=2, z=3
После: x=2, y=3, z=1
```

---

### **3️⃣ Обратный порядок**

📌 **Задание:**
Объявите четыре переменные `a, b, c, d` со значениями 1, 2, 3, 4. Расположите их в обратном порядке: `d, c, b, a`.

✅ **Пример работы:**

```
До: 1 2 3 4
После: 4 3 2 1
```

---

### **4️⃣ Копирование значений**

📌 **Задание:**
Объявите переменные `source` и `destination`. Присвойте `source` значение 42. Скопируйте значение из `source` в `destination`, затем измените `source` на 100. Проверьте, что `destination` осталось 42.

✅ **Пример работы:**

```
source = 42, destination = 42
После изменения: source = 100, destination = 42
```

---

### **5️⃣ Целочисленное деление**

📌 **Задание:**
Объявите переменные `dividend = 17` и `divisor = 5`. Вычислите целую часть от деления и остаток, используя только операции присваивания и арифметики.

✅ **Пример работы:**

```
17 / 5 = 3 (остаток 2)
```

---

### **6️⃣ Сумма цифр двузначного числа**

📌 **Задание:**
Дано двузначное число 47. Сохраните его в переменной `number`. Вычислите сумму его цифр, используя операции деления и остатка от деления.

✅ **Пример работы:**

```
Число: 47
Сумма цифр: 4 + 7 = 11
```

---

### **7️⃣ Обмен через сумму**

📌 **Задание:**
Объявите две переменные `p = 15` и `q = 25`. Поменяйте их значения, используя только сложение и вычитание.

✅ **Пример работы:**

```
До: p=15, q=25
После: p=25, q=15
```

---

### **8️⃣ Минуты в часы**

📌 **Задание:**
Дано количество минут (например, 135). Переведите в часы и минуты, используя целочисленное деление.

✅ **Пример работы:**

```
135 минут = 2 часа 15 минут
```

---

### **9️⃣ Инкремент по цепочке**

📌 **Задание:**
Объявите три переменные `i=0, j=0, k=0`. Присвойте `j` значение `i`, затем увеличьте `i` на 1. Присвойте `k` значение `j`, затем увеличьте `j` на 1. Выведите значения всех переменных.

✅ **Пример работы:**

```
i=1, j=1, k=0
```

---

### **🔟 Сдвиг значений**

📌 **Задание:**
Объявите пять переменных `v1=10, v2=20, v3=30, v4=40, v5=50`. Сдвиньте значения: `v5→v4→v3→v2→v1→v5`.

✅ **Пример работы:**

```
До: 10 20 30 40 50
После: 50 10 20 30 40
```

---

### **1️⃣1️⃣ Работа с отрицательными числами**

📌 **Задание:**
Объявите переменную `temp = -15`. Получите ее абсолютное значение, используя условное выражение (можно через if или тернарный оператор).

✅ **Пример работы:**

```
Исходное: -15
Абсолютное: 15
```

---

### **1️⃣2️⃣ Остаток от деления**

📌 **Задание:**
Для чисел `a = 23` и `b = 7` найдите остаток от деления `a` на `b`, не используя оператор `%`.

✅ **Пример работы:**

```
23 % 7 = 2
```

---

### **1️⃣3️⃣ Символьная арифметика**

📌 **Задание:**
Объявите переменную `symbol = 'A'`. Получите из нее символ 'C', используя арифметические операции с символами.

✅ **Пример работы:**

```
'A' → 'C'
```

---

### **1️⃣4️⃣ Четное/нечетное**

📌 **Задание:**
Проверьте, является ли число 24 четным, используя операцию остатка от деления. Результат сохраните в переменной.

✅ **Пример работы:**

```
24 - четное (остаток 0)
```

---

### **1️⃣5️⃣ Обмен побитовый**

📌 **Задание:**
Поменяйте значения переменных `m = 5` и `n = 3` с использованием операции XOR (^).

✅ **Пример работы:**

```
До: m=5, n=3
После: m=3, n=5
```

---

### **1️⃣6️⃣ Умножение через сложение**

📌 **Задание:**
Умножьте 6 на 4, используя только операцию сложения в цикле.

✅ **Пример работы:**

```
6 * 4 = 24
```

---

### **1️⃣7️⃣ Деление на 2**

📌 **Задание:**
Разделите число 18 на 2, используя битовый сдвиг.

✅ **Пример работы:**

```
18 / 2 = 9
```

---

### **1️⃣8️⃣ Инвертирование знака**

📌 **Задание:**
Инвертируйте знак числа `value = 12` без использования оператора умножения.

✅ **Пример работы:**

```
12 → -12
```

---

### **1️⃣9️⃣ Количество десятков**

📌 **Задание:**
Для числа 86 определите количество десятков, используя целочисленное деление.

✅ **Пример работы:**

```
86 → 8 десятков
```

---

### **2️⃣0️⃣ Циклический сдвиг трех переменных**

📌 **Задание:**
Даны переменные `alpha = 'a', beta = 'b', gamma = 'c'`. Сделайте циклический сдвиг вправо: `alpha→beta, beta→gamma, gamma→alpha`.

✅ **Пример работы:**

```
До: a b c
После: c a b
```

---
